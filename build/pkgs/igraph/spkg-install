if gcc -lxml2 2>&1 | grep -q 'cannot find -lxml2'; then
    sdh_die <<_EOF_
You need libxml2 to run igraph. On Ubuntu and Debian Linux, installing the
build-essential and the libxml2-dev packages is sufficient.
_EOF_
fi

cd src
sdh_configure
sdh_make
sdh_make_install -j1
