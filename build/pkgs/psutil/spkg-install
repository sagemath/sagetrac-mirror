#!/usr/bin/env bash

if [ "$UNAME" = "Darwin" ] && [ $MACOSX_VERSION -ge 16 ]; then
    echo "OS X 10.$[$MACOSX_VERSION-4] Building with clang."
    CC=clang
fi

if [ "$UNAME" = "CYGWIN" ]; then
    echo "psutil cannot currently be installed on Cygwin"
else
    cd src && $PIP_INSTALL .
fi
