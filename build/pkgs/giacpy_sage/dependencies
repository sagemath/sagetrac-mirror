$(PYTHON) cython giac cysignals |  $(PYTHON_TOOLCHAIN) $(and $(filter-out no,$(SAGE_CHECK_giacpy_sage)), $(SAGERUNTIME) ipywidgets sympy maxima)

spkg-check uses the sage command. The doctest suite (SAGE_CHECK=yes)  also uses Sympy and Maxima
The sage doctester depends on ipywidgets.
----------
All lines of this file are ignored except the first.
It is copied by SAGE_ROOT/build/install into SAGE_ROOT/build/Makefile.
