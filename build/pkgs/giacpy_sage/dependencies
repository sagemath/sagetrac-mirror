$(PYTHON) cython giac cysignals |  $(PYTHON_TOOLCHAIN) $(SAGERUNTIME) $(and $(filter-out no,$(SAGE_CHECK_giacpy_sage)), ipywidgets sympy maxima)

Installation (setup.py) needs sagelib. The doctest suite (SAGE_CHECK=yes) also uses Sympy and Maxima.
The sage doctester depends on ipywidgets.
----------
All lines of this file are ignored except the first.
It is copied by SAGE_ROOT/build/install into SAGE_ROOT/build/Makefile.
