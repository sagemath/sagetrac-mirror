#!/usr/bin/env bash

if [ "$SAGE_LOCAL" = "" ]; then
   echo "SAGE_LOCAL undefined ... exiting";
   echo "Maybe run 'sage -sh'?"
   exit 1
fi

cd src

# Apply patch as binary since source has incorrect line endings
sage-apply-patches -- --binary || exit 1

$PIP_INSTALL .

if [ $? -ne 0 ]; then
    echo "Error installing pyparsing ... exiting"
    exit 1
fi
