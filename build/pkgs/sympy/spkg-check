if [ -z "$SAGE_LOCAL" ]; then
    echo "Error: SAGE_LOCAL undefined - exiting..."
    echo "Maybe run 'sage -sh'?"
    exit 1
fi

sage -t --verbose src/sympy/external/tests/test_sage.py

if [ $? -ne 0 ]; then
    echo "There was a problem during the sympy tests."
    exit 1
fi

