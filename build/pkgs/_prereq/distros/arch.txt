binutils
make
m4
perl
python # system python for bootstrapping the build
tar
bc
gcc
# Needed for 4ti2:
which
# `make configure` needs automake (for the `aclocal` command) and autoconf
automake
autoconf
