diff -druN ecl-12.12.1.orig/src/configure.in ecl-12.12.1/src/configure.in
--- ecl-12.12.1.orig/src/configure.in	2012-12-17 02:08:10.000000000 -0800
+++ ecl-12.12.1/src/configure.in	2014-01-09 02:47:51.405883230 -0800
@@ -11,7 +11,6 @@
 AC_INIT([ecl],[12.12.1],[])
 AC_REVISION([$Revision$])
 AC_CONFIG_SRCDIR([bare.lsp.in])
-AC_CONFIG_AUX_DIR([gmp])
 AC_PREREQ(2.59)
 
 dnl -----------------------------------------------------------------------
diff -druN ecl-12.12.1.orig/src/configure ecl-12.12.1/src/configure
--- ecl-12.12.1.orig/src/configure	2012-12-17 02:08:11.000000000 -0800
+++ ecl-12.12.1/src/configure	2014-01-09 02:47:43.405882430 -0800
@@ -2544,7 +2544,7 @@
 
 
 ac_aux_dir=
-for ac_dir in gmp "$srcdir"/gmp; do
+for ac_dir in "$srcdir" "$srcdir/.." "$srcdir/../.."; do
   if test -f "$ac_dir/install-sh"; then
     ac_aux_dir=$ac_dir
     ac_install_sh="$ac_aux_dir/install-sh -c"
@@ -2560,7 +2560,7 @@
   fi
 done
 if test -z "$ac_aux_dir"; then
-  as_fn_error $? "cannot find install-sh, install.sh, or shtool in gmp \"$srcdir\"/gmp" "$LINENO" 5
+  as_fn_error $? "cannot find install-sh, install.sh, or shtool in \"$srcdir\" \"$srcdir/..\" \"$srcdir/../..\"" "$LINENO" 5
 fi
 
 # These three variables are undocumented and unsupported,
