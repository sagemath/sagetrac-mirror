#!/bin/sh

cp patches/make.inc src

cd src/

make lapacklib

if [ $? -ne 0 ]; then
    echo "Error compiling lapack."
    exit 1
fi

sage-dist-install -d "$DESTDIR$SAGE_LOCAL/lib"
sage-dist-install -t "$DESTDIR$SAGE_LOCAL/lib" lapack.a
