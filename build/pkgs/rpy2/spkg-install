if [ -z "$SAGE_LOCAL" ]; then
    echo >&2 "SAGE_LOCAL undefined ... exiting"
    echo >&2 "Maybe run 'sage --sh'?"
    exit 1
fi

set -e

cd src

if [ -v SAGE_R_LIB ]
then 
    export RHOMES="${SAGE_R_LIB}"
else
    export RHOMES="${SAGE_LOCAL}"/lib/R
fi

sdh_pip_install .
