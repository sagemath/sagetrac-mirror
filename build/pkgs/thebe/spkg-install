if [ -z "$SAGE_SHARE" ]; then
    echo >&2 "SAGE_SHARE undefined ... exiting"
    echo >&2 "Maybe run 'sage --sh'?"
    exit 1
fi

TARGET="${SAGE_INST_TEMP}${SAGE_SHARE}/thebe"
if [ ! -d "${TARGET}" ]; then
   mkdir -p "${TARGET}"
fi

cp 'src/static/main-built.js' "${TARGET}/thebe.js"

