#!/usr/bin/env bash

if [ -z "$SAGE_LOCAL" ]; then
    echo >&2 "SAGE_LOCAL undefined ... exiting"
    echo >&2 "Maybe run 'sage --sh'?"
    exit 1
fi

cd src
echo "building sage_guess"

$PIP_INSTALL .

if [ $? -ne 0 ]; then
    echo >&2 "Error building sage_guess"
    exit 1
fi

