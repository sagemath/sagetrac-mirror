#!/usr/bin/env python

import sys
from sage.all import *
from sage.calculus.predefined import x
from sage.misc.preparser import preparse_block

s = preparse_block(" ".join(sys.argv[1:]) + '\n')
eval(compile(s, '<cmdline>', 'single'))
