#!/bin/sh

if test -z "$FC"; then
    # Default value determined in ./configure
    myFC=@FC@
else
    myFC="$FC"
fi

# unset FC to avoid an infinite loop in the case that FC=sage_fortran.
unset FC

exec $myFC @FCFLAGS@ -fPIC "$@"
